{% extends 'base.html.twig' %}

{% block title %}Изменить курс{% endblock %}

{% block body %}
    <div class="d-flex flex-column mx-3">
        <h1>Изменить курс</h1>

        {{ include('course/_form.html.twig', {'button_label': 'Сохранить'}) }}

        <div class="d-flex mt-3">
            {{ include('course/_delete_form.html.twig') }}
        </div>
    </div>
    <a class="btn btn-outline-secondary m-3" role="button" href="{{ path('app_course_index') }}">Вернуться к курсу</a>

{% endblock %}
